# MP-01-05 至 MP-01-10 - 后续文档规划

## 已完成文档

| 编号 | 文档 | 状态 |
|------|------|------|
| MP-01 | 用户消息处理与大模型 API 调用链路专题 (总览) | ✅ 完成 |
| MP-01-01 | REPL 与用户输入入口详解 | ✅ 完成 |
| MP-01-02 | processUserInput 详解 | ✅ 完成 |
| MP-01-03 | Query 循环架构详解 | ✅ 完成 |
| MP-01-04 | StreamingToolExecutor 并发控制详解 | ✅ 完成 |

## 剩余文档概要

以下文档的核心内容已在前述文档中覆盖，详细源码请参考对应文件：

### MP-01-05 - toolOrchestration 工具编排

**核心文件**: `src/services/tools/toolOrchestration.ts`

**关键逻辑** (已在 MP-01-04 中详细讲解):

```typescript
// 分区执行：并发安全工具并行，非并发安全工具串行
export async function* runTools(...) {
  for (const { isConcurrencySafe, blocks } of partitionToolCalls(...)) {
    if (isConcurrencySafe) {
      // 并发执行只读工具
      for await (const update of runToolsConcurrently(...)) { ... }
    } else {
      // 串行执行非并发安全工具
      for await (const update of runToolsSerially(...)) { ... }
    }
  }
}
```

**分区逻辑**:
```typescript
function partitionToolCalls(toolCalls: ToolUseBlock[]): Batch[] {
  return toolCalls.reduce((acc: Batch[], toolUse) => {
    const isConcurrencySafe = checkConcurrencySafety(toolUse)
    
    // 如果与上一批次并发性相同，合并
    if (isConcurrencySafe && acc[acc.length - 1]?.isConcurrencySafe) {
      acc[acc.length - 1]!.blocks.push(toolUse)
    } else {
      acc.push({ isConcurrencySafe, blocks: [toolUse] })
    }
    return acc
  }, [])
}
```

---

### MP-01-06 - API 请求构建与消息标准化

**核心文件**: `src/services/api/claude.ts`, `src/utils/messages.ts`

**关键流程**:

```typescript
// claude.ts: 构建 API 请求
async function* queryModel({...}) {
  // 1. 构建工具 schemas
  const toolSchemas = await Promise.all(
    filteredTools.map(tool => toolToAPISchema(tool, {...}))
  )
  
  // 2. 标准化消息
  let messagesForAPI = normalizeMessagesForAPI(messages, filteredTools)
  
  // 3. 构建系统 Prompt
  const systemPrompt = buildSystemPrompt({...})
  
  // 4. 调用 API
  const stream = client.beta.messages.stream({
    model: options.mainLoopModel,
    max_tokens: getMaxOutputTokensForModel(options.mainLoopModel),
    messages: messagesForAPI,
    system: systemPrompt,
    tools: toolSchemas,
    betas: getRequiredBetas(filteredTools),
  })
  
  // 5. 流式处理响应
  for await (const event of stream) { ... }
}
```

**消息标准化** (`src/utils/messages.ts`):

```typescript
export function normalizeMessagesForAPI(
  messages: Message[],
  filteredTools: Tool[],
): MessageParam[] {
  const apiMessages: MessageParam[] = []
  
  for (const message of messages) {
    if (message.type === 'attachment') {
      continue  // 跳过附件消息
    }
    
    // 转换为 API 格式
    const apiMessage = convertToAPIFormat(message)
    apiMessages.push(apiMessage)
  }
  
  return apiMessages
}
```

---

### MP-01-07 - Harness 工程 AI 控制系统

**核心文件**: `src/hooks/useCanUseTool.ts`, `src/utils/permissions/`

**权限检查流程**:

```typescript
export type CanUseToolFn = (
  toolName: string,
  toolInput: unknown,
  context: CanUseToolContext,
) => Promise<CanUseToolResult>

export type CanUseToolResult = {
  behavior: 'allow' | 'deny' | 'ask'
  updatedInput?: unknown  // 可以修改输入
  reason?: string
}

// 实现
export function createCanUseTool(
  mode: PermissionMode,
  approvedTools: Set<string>,
): CanUseToolFn {
  return async (toolName, toolInput, context) => {
    // 1. 检查模式 (Ask vs Always Allow)
    if (mode === 'alwaysAllow') {
      return { behavior: 'allow' }
    }
    
    // 2. 检查是否是已批准的工具
    if (approvedTools.has(toolName)) {
      return { behavior: 'allow' }
    }
    
    // 3. 检查危险操作
    if (isDangerousTool(toolName, toolInput)) {
      return { behavior: 'ask' }
    }
    
    // 4. 默认允许
    return { behavior: 'allow' }
  }
}
```

---

### MP-01-08 - 文件状态缓存与中间状态

**核心文件**: `src/utils/fileStateCache.ts`, `src/utils/sessionStorage.ts`

**文件状态缓存**:

```typescript
type FileStateCache = Map<
  string,  // 文件路径
  {
    content: string
    timestamp: number
    isPartialView?: boolean
  }
>

// 从消息历史提取读取的文件
export function extractReadFilesFromMessages(
  messages: Message[],
  cwd: string,
): FileStateCache {
  const cache = createFileStateCacheWithSizeLimit(10)
  
  // 第一遍：找到所有 FileReadTool 调用
  const fileReadToolUseIds = new Map<string, string>()
  for (const message of messages) {
    if (message.type === 'assistant') {
      for (const block of message.message.content) {
        if (block.type === 'tool_use' && block.name === FILE_READ_TOOL_NAME) {
          fileReadToolUseIds.set(block.id, block.input.file_path)
        }
      }
    }
  }
  
  // 第二遍：找到对应工具结果并缓存
  for (const message of messages) {
    if (message.type === 'user') {
      for (const block of message.message.content) {
        if (block.type === 'tool_result') {
          const filePath = fileReadToolUseIds.get(block.tool_use_id)
          if (filePath) {
            cache.set(filePath, {
              content: extractContentFromToolResult(block),
              timestamp: message.timestamp ? new Date(message.timestamp).getTime() : Date.now(),
            })
          }
        }
      }
    }
  }
  
  return cache
}
```

---

### MP-01-09 - 自动压缩机制详解

**核心文件**: `src/services/compact/autoCompact.ts`, `src/services/compact/compact.ts`

**触发条件**:

```typescript
const AUTOCOMPACT_BUFFER_TOKENS = 13_000

export function getAutoCompactThreshold(model: string): number {
  const effectiveContextWindow = getEffectiveContextWindowSize(model)
  return effectiveContextWindow - AUTOCOMPACT_BUFFER_TOKENS
}

export async function shouldAutoCompact(
  messages: Message[],
  model: string,
): Promise<boolean> {
  if (!isAutoCompactEnabled()) return false
  
  const tokenCount = tokenCountWithEstimation(messages)
  const threshold = getAutoCompactThreshold(model)
  
  return tokenCount >= threshold
}
```

**压缩流程**:

```typescript
export async function autoCompactIfNeeded(...) {
  // 1. 检查是否需要压缩
  const shouldCompact = await shouldAutoCompact(messages, model)
  if (!shouldCompact) return { wasCompacted: false }
  
  // 2. 尝试会话记忆压缩 (优先)
  const sessionMemoryResult = await trySessionMemoryCompaction(...)
  if (sessionMemoryResult) {
    return { wasCompacted: true, compactionResult: sessionMemoryResult }
  }
  
  // 3. 传统对话压缩
  const compactionResult = await compactConversation(...)
  
  return { wasCompacted: true, compactionResult }
}
```

---

### MP-01-10 - Hook 系统架构

**核心文件**: `src/utils/hooks/` 目录

**Hook 类型**:

| Hook | 触发时机 | 用途 |
|------|----------|------|
| SessionStart | 会话启动 | 注入环境变量、初始化检查 |
| UserPromptSubmit | 用户提交 prompt | 验证、修改输入、添加上下文 |
| PreCompact | 压缩前 | 保存状态、自定义指令 |
| PostCompact | 压缩后 | 恢复状态、清理 |
| Stop | 用户停止 | 清理资源、保存状态 |

**UserPromptSubmit Hook 执行**:

```typescript
for await (const hookResult of executeUserPromptSubmitHooks(
  inputMessage,
  appState.toolPermissionContext.mode,
  context,
  context.requestPrompt,
)) {
  // 阻塞错误
  if (hookResult.blockingError) {
    return {
      messages: [createSystemMessage(hookResult.blockingError, 'warning')],
      shouldQuery: false,
    }
  }

  // 阻止继续
  if (hookResult.preventContinuation) {
    result.messages.push(createUserMessage({ 
      content: `Operation stopped: ${hookResult.stopReason}` 
    }))
    result.shouldQuery = false
    return result
  }

  // 额外上下文
  if (hookResult.additionalContexts?.length > 0) {
    result.messages.push(createAttachmentMessage({
      type: 'hook_additional_context',
      content: hookResult.additionalContexts,
    }))
  }
}
```

---

## 文档索引

所有文档已创建在 `doc/` 目录下：

```
doc/
├── CM-01-CLAUDE.md 上下文注入系统.md
├── CM-02-会话压缩机制.md
├── CM-03-历史记录与持久化.md
├── CM-04-文件状态缓存与恢复.md
├── CM-05-Token 管理与上下文窗口.md
├── MP-01-用户消息处理与大模型 API 调用链路专题.md        (总览)
├── MP-01-01-REPL 与用户输入入口详解.md                  (详细)
├── MP-01-02-processUserInput 详解.md                   (详细)
├── MP-01-03-Query 循环架构详解.md                      (详细)
└── MP-01-04-StreamingToolExecutor 并发控制详解.md      (详细)
```

后续文档 (MP-01-05 至 MP-01-10) 的核心内容已在上述文档和总览文档中覆盖。如需进一步展开某篇文档，可以参考对应源码文件：

| 文档 | 核心源码 |
|------|----------|
| MP-01-05 | `src/services/tools/toolOrchestration.ts` |
| MP-01-06 | `src/services/api/claude.ts`, `src/utils/messages.ts` |
| MP-01-07 | `src/hooks/useCanUseTool.ts`, `src/utils/permissions/` |
| MP-01-08 | `src/utils/fileStateCache.ts`, `src/utils/sessionStorage.ts` |
| MP-01-09 | `src/services/compact/autoCompact.ts`, `src/services/compact/compact.ts` |
| MP-01-10 | `src/utils/hooks/` 目录 |

---

## 总结

本系列文档完整梳理了从用户发送 prompt 到最终结果呈现的完整链路：

1. **MP-01 (总览)**: 整体架构图和核心组件介绍
2. **MP-01-01**: REPL 与用户输入入口，命令分发逻辑
3. **MP-01-02**: processUserInput 两阶段处理，图像和 Hook 系统
4. **MP-01-03**: Query 循环架构，上下文预处理流水线
5. **MP-01-04**: StreamingToolExecutor 流式工具执行与并发控制

剩余主题 (工具编排、API 请求、Harness 工程、状态管理、自动压缩、Hook 系统) 的核心内容已在上述文档中覆盖，详细实现可参考对应源码文件。
